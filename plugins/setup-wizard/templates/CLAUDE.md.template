<!-- This file loads on every Claude turn — keep it concise. -->
<!-- Move directory-scoped rules to .claude/rules/<area>.md to save tokens. -->

# {{PROJECT_NAME}}

{{PROJECT_PURPOSE}}

**Stage:** {{STAGE}} · **Team:** {{TEAM_SIZE}} · **Primary chat language:** {{CHAT_LANGUAGE}}

---

## Stack

- **Languages & frameworks:** {{STACK}}
- **Package manager:** {{PACKAGE_MANAGER}}
- **Linter / formatter:** {{LINTER}}
- **Test runner:** {{TEST_RUNNER}}

## Commands

Claude may run these commands directly. Anything destructive or not listed here
requires confirmation.

| Purpose         | Command              |
|-----------------|----------------------|
| Build           | `{{BUILD_CMD}}`      |
| Dev / run       | `{{DEV_CMD}}`        |
| Test            | `{{TEST_CMD}}`       |
| Lint / format   | `{{LINT_CMD}}`       |
| Deploy          | `{{DEPLOY_CMD}}`     |
| DB / migrations | `{{DB_CMD}}`         |

## Architecture

{{ARCHITECTURE_SUMMARY}}

### Modules

{{MODULES_LIST}}

### Nested context

Sub-areas have their own `CLAUDE.md` files that extend this one:
{{NESTED_AREAS}}

### Scoped rules

Directory-specific rules live in `.claude/rules/` and load only when relevant:
{{SCOPED_RULES}}

## Rules

### Claude must ALWAYS
{{ALWAYS_RULES}}

### Claude must NEVER
{{NEVER_RULES}}

### Known pitfalls
{{PITFALLS}}

## Secrets & environments

{{SECRETS_POLICY}}

**Files Claude may read:** {{READABLE_ENV}}
**Files Claude must NEVER touch:** {{FORBIDDEN_ENV}}

## External context

{{EXTERNAL_APIS}}

## Style

- **Commits:** {{COMMIT_STYLE}}
- **Documentation:** {{DOC_STYLE}}
- **Response verbosity:** {{VERBOSITY}}

---

<!-- Generated by claude-setup-wizard. Safe to edit by hand. -->
<!-- Re-run `/audit` to check what's missing. -->
